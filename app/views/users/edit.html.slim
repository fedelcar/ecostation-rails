= image_tag('im3.jpg', class: 'bg')
.container-fluid.margin-top.col-md-offset-2.col-md-8.container.text-center
  .row
    .text-center
      h1.green-label Editar Perfil
  .row
    .col-md-4.col-md-offset-4.text-center
      = simple_form_for @user do |f|
        = f.input :first_name
        = f.input :last_name
        = f.input :dni
        = f.input :email
        = f.input :password
        = f.button :button do
          | Guardar Cambios
        end
